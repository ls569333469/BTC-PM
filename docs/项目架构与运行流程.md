# BTC Chanlun Analyzer — 项目架构与运行流程

## 1. 系统总览

```mermaid
graph TB
    subgraph External["外部数据源"]
        Binance["Binance API<br/>K线/资金费率/持仓量"]
        AlterMe["Alternative.me<br/>恐惧贪婪指数"]
        Gamma["Polymarket Gamma API<br/>预测市场盘口"]
        Chainlink["Chainlink Data Stream<br/>BTC/USD 基准价"]
    end

    subgraph Backend["后端 · FastAPI + Python"]
        Main["main.py<br/>应用入口"]
        Scheduler["APScheduler<br/>定时任务"]
        WS["WebSocket<br/>实时推送"]
        
        subgraph APIs["API 路由层"]
            ChanlunAPI["/api/chanlun"]
            BacktestAPI["/api/backtest"]
            PolyAPI["/api/polymarket-prices"]
            CronAPI["/api/cron"]
        end
        
        subgraph Services["服务层"]
            ChanlunSvc["ChanlunService<br/>缠论分析"]
            BacktestSvc["BacktestService<br/>回测统计"]
            PolySvc["PolymarketService<br/>PM数据处理"]
        end
        
        subgraph Engines["缠论引擎"]
            Bi["bi.py<br/>笔识别"]
            ZS["zhongshu.py<br/>中枢构建"]
            Div["divergence.py<br/>背驰检测"]
            Trend["trend.py<br/>趋势分析"]
            Pred["prediction.py<br/>多级别预测"]
            Score["scoring.py<br/>双因子评分"]
        end
        
        DB[("PostgreSQL<br/>预测记录")]
    end

    subgraph Frontend["前端 · React + Vite"]
        App["App.tsx<br/>主页面"]
        RQ["TanStack Query<br/>数据缓存"]
        WSHook["useWebSocket<br/>实时推送"]
        
        subgraph Components["UI 组件"]
            MO["MarketOverview<br/>市场概览"]
            PC["PriceChart<br/>价格K线+缠论"]
            PT["PredictionTable<br/>多时间框架预测"]
            PG["PolymarketGuide<br/>投注指南"]
            BP["BacktestPanel<br/>回测面板"]
            WR["WinRateChart<br/>胜率图表"]
        end
    end

    Binance --> ChanlunSvc
    AlterMe --> ChanlunSvc
    Gamma --> PolySvc
    Chainlink -.->|priceToBeat| Gamma

    ChanlunSvc --> Bi --> ZS --> Div --> Trend --> Pred --> Score
    ChanlunAPI --> ChanlunSvc
    BacktestAPI --> BacktestSvc
    PolyAPI --> PolySvc
    BacktestSvc --> DB
    
    Scheduler -->|每15分钟| ChanlunSvc
    Scheduler -->|每5分钟| BacktestSvc
    ChanlunSvc -->|广播结果| WS
    
    WS -->|实时推送| WSHook
    RQ -->|HTTP轮询| APIs
    WSHook -->|更新缓存| RQ
    RQ --> Components
```

---

## 2. 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **后端框架** | FastAPI + Uvicorn | 异步 Python Web 框架 |
| **定时任务** | APScheduler | 周期性分析 + 自动结算 |
| **实时通信** | WebSocket | 分析结果推送到前端 |
| **数据库** | PostgreSQL + SQLAlchemy | 预测记录持久化 |
| **HTTP客户端** | httpx | 异步外部 API 调用 |
| **前端框架** | React 18 + Vite | SPA 单页应用 |
| **状态管理** | TanStack Query | API 缓存 + 自动轮询 |
| **图表** | Recharts | 价格走势 + 胜率可视化 |
| **样式** | Tailwind CSS | 暗色主题 UI |

---

## 3. 后端运行流程

### 3.1 启动流程

```mermaid
sequenceDiagram
    participant U as Uvicorn
    participant M as main.py
    participant DB as PostgreSQL
    participant S as APScheduler

    U->>M: 创建 FastAPI app
    M->>DB: init_db() 初始化表结构
    M->>S: 创建调度器
    M->>S: 注册 auto_analysis (15min)
    M->>S: 注册 auto_resolve (5min)
    S->>S: scheduler.start()
    M->>U: 注册 5 个路由组
    Note over U: 服务就绪 :8000
```

### 3.2 核心分析流程 (每15分钟)

```mermaid
flowchart TD
    A["⏰ APScheduler 触发<br/>auto_analysis()"] --> B["并行获取外部数据"]
    
    B --> B1["Binance: 168根 1H K线"]
    B --> B2["Binance: 资金费率"]
    B --> B3["Alternative.me: 恐惧贪婪"]
    B --> B4["Binance: 持仓量"]
    
    B1 & B2 & B3 & B4 --> C["本地计算技术指标<br/>RSI / MACD / 布林带"]
    
    C --> D["缠论引擎流水线"]
    
    subgraph Engine["缠论引擎 (本地计算)"]
        D --> E1["bi.py → 识别笔 (顶底分型)"]
        E1 --> E2["zhongshu.py → 构建中枢"]
        E2 --> E3["divergence.py → 检测背驰"]
        E3 --> E4["trend.py → 判断趋势"]
        E4 --> E5["prediction.py → 7级别预测<br/>30M/1H/2H/4H/8H/12H/24H"]
        E5 --> E6["scoring.py → 双因子评分<br/>缠论因子 + 市场因子"]
    end
    
    E6 --> F{"有 WS 客户端?"}
    F -->|是| G["广播到所有 WebSocket 客户端"]
    F -->|否| H["跳过广播"]
    
    E6 --> I["保存预测到 PostgreSQL"]
    I --> J["等待下一轮 (15min)"]

    style A fill:#1a1a2e,stroke:#e94560,color:#fff
    style Engine fill:#16213e,stroke:#0f3460
```

### 3.3 Polymarket 数据流

```mermaid
flowchart TD
    A["前端请求<br/>GET /api/polymarket-prices/prices"] --> B["PolymarketService.get_prices()"]
    
    B --> C["获取缠论预测<br/>ChanlunService.full_analysis()"]
    C --> D["并行获取 5 个时间框架"]
    
    D --> D1["_fetch_15m()<br/>slug: btc-updown-15m-{ts}"]
    D --> D2["_fetch_1h()<br/>slug: bitcoin-up-or-down-{月}-{日}-{年}-{时}{am/pm}-et"]
    D --> D3["_fetch_4h()<br/>slug: btc-updown-4h-{ts}"]
    D --> D4["_fetch_daily()<br/>slug: bitcoin-up-or-down-on-{月}-{日}-{年}"]
    D --> D5["_fetch_weekly()<br/>slug: bitcoin-above-on-{月}-{日}"]
    
    D1 & D2 & D3 & D4 & D5 --> E["_fetch_event_by_slug(slug)"]
    E --> F["Gamma API<br/>GET /events?slug=X"]
    
    F --> G["解析返回数据"]
    G --> G1["outcomes/outcomePrices<br/>json.loads() 解析"]
    G --> G2["eventMetadata.priceToBeat<br/>Chainlink BTC/USD 快照价"]
    
    G1 & G2 --> H["_generate_guides()<br/>合并缠论预测 + PM数据"]
    
    H --> I["返回 JSON<br/>timeframes[] + guides[]"]

    style A fill:#1a1a2e,stroke:#e94560,color:#fff
    style F fill:#16213e,stroke:#0f3460,color:#fff
```

### 3.4 自动结算流程 (每5分钟)

```mermaid
flowchart LR
    A["⏰ auto_resolve()"] --> B["查询已过期预测<br/>prediction_time + TTL < now"]
    B --> C["获取 BTC 当前价格"]
    C --> D["逐条比较方向+目标价"]
    D --> E["标记 HIT/MISS/EXACT/CLOSE"]
    E --> F["写回 DB + 更新统计"]
```

---

## 4. 前端运行流程

### 4.1 数据获取策略

```mermaid
flowchart TD
    subgraph Polling["HTTP 轮询 (TanStack Query)"]
        P1["useChanlunAnalysis()<br/>每 15 分钟"]
        P2["usePolymarketPrices()<br/>每 5 分钟"]
        P3["useBacktestStats()<br/>每 1 分钟"]
    end
    
    subgraph Push["WebSocket 推送"]
        WS["useWebSocket()"]
        WS -->|"type: analysis"| Cache["queryClient.setQueryData()<br/>直接更新缓存"]
    end
    
    P1 & P2 & P3 --> RQ["TanStack Query 缓存"]
    Cache --> RQ
    
    RQ --> App["App.tsx"]
    App --> C1["MarketOverview<br/>BTC价格/趋势/RSI/恐惧贪婪"]
    App --> C2["PriceChart<br/>K线 + 笔 + 中枢叠加"]
    App --> C3["PredictionChart<br/>目标价/支撑/阻力"]
    App --> C4["WinRateChart<br/>各时间框架胜率"]
    App --> C5["PredictionTable<br/>7级别预测 + PM盘口"]
    App --> C6["PolymarketGuide<br/>5级别投注指南"]
    App --> C7["BacktestPanel<br/>回测统计"]
    App --> C8["TriggerPanel<br/>关键触发因素"]
```

### 4.2 页面布局结构

```
┌─────────────────────────────────────────────────────┐
│  Header: BTC Chanlun Analyzer · POLYMARKET GUIDE    │
├─────────────────────────────────────────────────────┤
│  MarketOverview: 价格 | 趋势 | RSI | 恐惧贪婪 | 费率│
├─────────────────────────────────────────────────────┤
│  PriceChart: K线 + 缠论笔 + 中枢覆盖                │
├───────────────────────┬─────────────────────────────┤
│  PredictionChart      │  WinRateChart               │
│  目标价/支撑/阻力      │  各时间框架胜率柱状图         │
├───────────────────────┴─────────────────────────────┤
│  PredictionTable: 多时间框架预测表                    │
│  时间周期 | 信号 | PM盘口 | PM信号 | 目标价 | 胜率    │
├─────────────────────────────────────────────────────┤
│  PolymarketGuide: Polymarket 投注指南                │
│  开盘基准价 | 当前偏移 | 确定概率 | 缠论胜率 | 市场概率 │
├───────────────────────┬─────────────────────────────┤
│  TriggerPanel         │  BacktestPanel              │
│  关键触发因素          │  回测统计                    │
└───────────────────────┴─────────────────────────────┘
```

---

## 5. API 端点清单

| 端点 | 方法 | 说明 | 轮询间隔 |
|------|------|------|----------|
| `/api/chanlun/analysis` | GET | 完整缠论分析 + 7级别预测 | 15min |
| `/api/chanlun/validate` | GET | 验证上一轮预测准确率 | 15min |
| `/api/backtest/stats` | GET | 回测统计 (胜率/精度) | 1min |
| `/api/backtest/save` | POST | 保存预测批次 | 自动 |
| `/api/backtest/resolve` | POST | 手动结算过期预测 | — |
| `/api/polymarket-prices/prices` | GET | PM盘口 + 投注指南 | 5min |
| `/api/cron/` | GET | 查看定时任务列表 | — |
| `/api/cron/{job_id}/run` | POST | 手动触发定时任务 | — |
| `/api/health` | GET | 健康检查 | — |
| `/ws/analysis` | WS | 实时分析推送 | 持续连接 |

---

## 6. 外部 API 依赖

| API | 用途 | 频率 | 需要密钥 |
|-----|------|------|---------|
| **Binance REST** | K线/资金费率/持仓量 | 每15min | ❌ 公开 |
| **Alternative.me** | 恐惧贪婪指数 | 每15min | ❌ 公开 |
| **Polymarket Gamma** | 预测市场盘口/概率 | 每5min | ❌ 公开 |
| **Chainlink Data Stream** | BTC/USD 基准价 (via Gamma `eventMetadata.priceToBeat`) | 每5min | ❌ 内嵌 |

---

## 7. 数据模型

### prediction 表 (PostgreSQL)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | SERIAL PK | 自增主键 |
| timeframe | VARCHAR | 时间级别 (30M/1H/...) |
| direction | VARCHAR | 预测方向 (up/down/sideways) |
| target_price | FLOAT | 目标价格 |
| current_price | FLOAT | 当时价格 |
| actual_price | FLOAT | 到期实际价格 |
| prediction_time | TIMESTAMP | 预测时间 |
| resolved_at | TIMESTAMP | 结算时间 |
| direction_correct | BOOLEAN | 方向是否正确 |
| accuracy_grade | VARCHAR | EXACT/CLOSE/HIT/MISS |
| error_pct | FLOAT | 误差百分比 |

---

## 8. 缠论引擎详解

```mermaid
flowchart TD
    K["168根 1H K线"] --> BI["bi.py: 笔识别"]
    BI -->|"顶底分型 → 笔序列"| ZS["zhongshu.py: 中枢构建"]
    ZS -->|"3笔重叠 → 中枢区间"| DIV["divergence.py: 背驰检测"]
    DIV -->|"MACD面积对比"| TR["trend.py: 趋势分析"]
    TR -->|"bullish / bearish / consolidating"| PR["prediction.py: 多级别预测"]
    
    PR --> PR1["30M: ±0.05% 震荡范围"]
    PR --> PR2["1H: ±0.1%"]
    PR --> PR3["2H: ±0.15%"]
    PR --> PR4["4H: ±0.25%"]
    PR --> PR5["8H: ±0.35%"]
    PR --> PR6["12H: ±0.5%"]
    PR --> PR7["24H: ±0.7%"]
    
    PR1 & PR2 & PR3 & PR4 & PR5 & PR6 & PR7 --> SC["scoring.py: 双因子评分"]
    
    SC --> F1["缠论因子<br/>趋势强度 + 中枢位置 + 背驰"]
    SC --> F2["市场因子<br/>RSI + 恐惧贪婪 + 资金费率"]
    F1 & F2 --> WR["综合胜率 (加权平均)"]
```

---

## 9. 关键目录结构

```
project/
├── backend/                    # Python 后端
│   ├── app/
│   │   ├── main.py            # 应用入口 + 生命周期
│   │   ├── config.py          # Pydantic Settings
│   │   ├── api/               # 路由层
│   │   │   ├── chanlun.py     #   缠论分析接口
│   │   │   ├── backtest.py    #   回测接口
│   │   │   ├── polymarket.py  #   PM价格接口
│   │   │   ├── cron.py        #   定时任务管理
│   │   │   └── ws.py          #   WebSocket 实时推送
│   │   ├── services/          # 业务逻辑层
│   │   │   ├── chanlun_service.py    # 缠论分析编排
│   │   │   ├── backtest_service.py   # 回测统计
│   │   │   └── polymarket_service.py # PM数据处理
│   │   ├── engines/           # 缠论计算引擎
│   │   │   ├── bi.py          #   笔识别
│   │   │   ├── zhongshu.py    #   中枢构建
│   │   │   ├── divergence.py  #   背驰检测
│   │   │   ├── trend.py       #   趋势分析
│   │   │   ├── prediction.py  #   多级别预测
│   │   │   └── scoring.py     #   综合评分
│   │   ├── clients/           # 外部API客户端
│   │   │   ├── binance_client.py     # Binance
│   │   │   ├── market_client.py      # 恐惧贪婪
│   │   │   └── polymarket_client.py  # Gamma API
│   │   └── models/            # 数据模型
│   │       ├── base.py        #   DB连接+会话
│   │       └── prediction.py  #   Prediction ORM
│   └── .env                   # 环境变量
│
├── frontend/                   # React 前端
│   ├── src/
│   │   ├── App.tsx            # 主页面布局
│   │   ├── components/        # UI 组件
│   │   │   ├── MarketOverview.tsx     # 市场概览
│   │   │   ├── PriceChart.tsx         # K线+缠论叠加
│   │   │   ├── PredictionChart.tsx    # 目标价投影
│   │   │   ├── WinRateChart.tsx       # 胜率柱状图
│   │   │   ├── PredictionTable.tsx    # 多TF预测表
│   │   │   ├── PolymarketGuide.tsx    # PM投注指南
│   │   │   ├── PolymarketPanel.tsx    # PM盘口展示
│   │   │   ├── BacktestPanel.tsx      # 回测统计面板
│   │   │   ├── TriggerPanel.tsx       # 触发因素
│   │   │   └── ValidationPanel.tsx    # 验证面板
│   │   ├── hooks/
│   │   │   └── useWebSocket.ts # WS实时推送Hook
│   │   └── lib/
│   │       ├── chanlun.ts     # API调用+React Query
│   │       ├── api.ts         # 基础HTTP封装
│   │       └── fetch.ts       # Fetch工具
│   └── index.html
│
└── docs/                       # 文档
```

---

## 10. 运行命令

```bash
# 后端
cd backend
pip install -r requirements.txt
cp .env.example .env            # 配置 DATABASE_URL
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 前端
cd frontend
npm install
npx vite --port 5173
```

**访问**: `http://localhost:5173`
